# Normalize line endings so a clone on any platform matches the repository.
* text=auto eol=lf

# Windows-only operator scripts keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary-ish assets must never be converted.
*.png binary
*.jpg binary
*.ico binary
*.exe binary
